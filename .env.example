# Frontend agents: use this base URL for all Typenx Core API calls and include credentials so the backend HTTP-only session cookie is sent.
VITE_TYPENX_API_BASE_URL=http://127.0.0.1:8080
VITE_TYPENX_AUTH_CALLBACK_PATH=/auth/callback
